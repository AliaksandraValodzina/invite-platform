node_modules/
.next/
.env*.local
.vercel
# The local object store. Uploaded bytes are content addressed and reproducible
# from a seed, so nothing here is worth committing.
.uploads/

test-results/
playwright-report/

# Generated by `next typegen` and `next build`. It references files under .next,
# so committing it would break a typecheck on a fresh checkout.
next-env.d.ts
*.tsbuildinfo
